/*
#include<iostream>
using namespace std;

int factorial(int num) {
    if (num == 0) return 0;
    int result = 1;

    for (int i = num; i >= 1; i--) {
        result *= i;
    }

    return result;
}


int main(void) {
    int n, k;

    cin >> n >> k;

    cout << factorial(n) / (factorial(k) * factorial(n - k));

    return 0;
}
*/